####~/.ssh/config###

# Default SSH config for github.com
Host github.com
  HostName github.com
  IdentityFile ~/.ssh/<ssh-private-key>

# ALIAS specific SSH config
Host <alias-name>
  HostName github.com 
  IdentityFile ~/.ssh/<ssh-private-key>

# NOTE: aliasing host breaks 'browse' (alias: 'bro') function in profile/navigation.sh
# TODO: use a different path, or make 'brows' smarter about host aliases

chmod 600 ~/.ssh/config

###CONFIG GIT REPO###

# default usage
git remote add origin git@github.com:<user>/<repo>.git

#alias usage
git remote add <whatever> git@<alias-name>:<user>/<repo>.git

### SOURCE TREE AUTHENTICATION FAILURE FIX ###
Sourcetree > Preferences > Git > Use System Git