# --- CORE & TAPS EQUIVALENTS ---
build-essential
curl
git
git-lfs
wget
gnupg
software-properties-common

# --- CONVENIENCE & SMALL APPS ---
ansible
jq
tldr
htop
bash-completion
speedtest-cli
# hstr (Requires PPA, skipping for standard)
shellcheck
# wezterm (Requires separate repo)

# --- LANGUAGES & BIG DEPENDENCIES ---
bash
# rbenv (Available in apt, but usually better via git-clone. Including for parity)
rbenv
golang-go
# gcc included in build-essential
python3
python3-pip
python3-venv
awscli
# azure-cli (Requires MS repo)
# cf-cli (Requires Cloudfoundry repo)

libglib2.0-bin
graphviz
# helm (Requires separate repo)
gradle
mysql-client
# pyenv (Better via git-installer)
pipenv
pipx
postgresql-client
# sbt (Requires Scala repo)
scala
npm
vim
openvpn
sed
# mongodb (Requires Mongo repo)
ruby-full

# --- INFRASTRUCTURE ---
# chamber (Manual binary install)
direnv
# tfenv (Manual install)
# terraform-docs (Manual install)
# pre-commit (Install via pip: pip install pre-commit)
# pinentry-mac -> Linux equivalent:
pinentry-tty
# circleci (Manual install)
# snyk (Manual install)
# nomad (Requires Hashicorp repo)
podman
# pkl (Manual install)
qemu-kvm
zstd
# gh (Requires GitHub repo setup)

# --- PACKETS ---
tcpdump
wireshark
tcpreplay

# --- GUI APPS (Available in Apt or 1Password Repo) ---
# Most Casks (VSCode, Slack, Spotify) are better installed via Snap/Flatpak on Linux.
# However, since newcomp.sh adds the 1Password repo, we CAN install these:
1password-cli
1password

# --- JAVA ---
# temurin replacement (OpenJDK is the standard Linux equivalent)
openjdk-17-jdk
groovy

# --- DOCKER ---
docker.io
docker-compose

# --- VPN ---
# openvpn (Listed above)
# aws-vpn-client (Proprietary .deb download required)

# --- MISC ---
# asciiquarium (Snap usually)
# vagrant (Available but often outdated)
vagrant